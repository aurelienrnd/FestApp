import { Router } from "express";

const router = Router();

// Programmation (front-office)
//router.get("/programming", notImplemented); // Voir la programmation (liste concerts)

export default router;
