import { Router } from "express";

const router = Router();

// Articles (front-office)
//router.get("/articles", notImplemented); // Voir la liste des articles
//router.get("/articles/:slug", notImplemented); // Lire un article

export default router;
