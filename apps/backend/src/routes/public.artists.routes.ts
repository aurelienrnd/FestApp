import { Router } from "express";

const router = Router();

// Artistes (front-office)
//router.get("/artists/:slug", notImplemented); // Voir fiche artiste
//router.get("/programming/artists/:id", notImplemented); // Acces depuis programmation

export default router;
