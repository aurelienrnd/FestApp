import { Router } from "express";

const router = Router();

// Articles (back-office)
//router.get("/articles", notImplemented); // Lister les articles
//router.post("/articles", notImplemented); // Creer un article
//router.get("/articles/:id", notImplemented); // Lire un article
//router.put("/articles/:id", notImplemented); // Modifier un article
//router.delete("/articles/:id", notImplemented); // Desactiver un article //NOTE a verifier l'utilisation de delate
export default router;
