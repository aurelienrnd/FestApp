import { Router } from "express";

const router = Router();

// Contact
//router.post("/submit", notImplemented); // Validation formulaire de contact
//router.get("/confirmation", notImplemented); // Page confirmation apres envoi

export default router;
